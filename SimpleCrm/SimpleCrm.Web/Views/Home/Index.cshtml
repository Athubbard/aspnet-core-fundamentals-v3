@model HomePageViewModel
@{
    ViewBag.Title = "Customers";
    
}
    <h1>Customers</h1>
    


   

        @foreach (var customer in @Model.Customers)
         {
              <partial name" _Summary" model="customer"></partial>
         }
    
       
        <a asp-action="Create">Create</a>
        
            @section footer {
               <h3>@Model.CurrentMessage</h3>
            }
        
    
